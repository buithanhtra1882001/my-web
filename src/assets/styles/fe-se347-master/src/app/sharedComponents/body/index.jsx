import React from "react";
import styled from "styled-components";
import tw from "twin.macro";

export const Body = styled.div`
    ${tw`
        flex flex-col items-start pt-10 absolute top-[40px] md:top-[137px] left-0 right-0 w-full
    `}
`
